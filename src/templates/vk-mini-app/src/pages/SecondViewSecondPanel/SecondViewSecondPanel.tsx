import { observer } from 'mobx-react';
import * as React from 'react';

const SecondViewSecondPanel = () => {
  return (
    <div>
      <h1>2-2</h1>
    </div>
  );
};

export default observer(SecondViewSecondPanel);
